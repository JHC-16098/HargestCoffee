import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hargest.coffee',
  appName: 'Hargest Coffee Company',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
